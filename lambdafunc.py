lambda x, y: x + y
# This is a simple lambda function that takes two arguments x and y, and returns their sum.
# You can use this lambda function like this:
# result = (lambda x, y: x + y)(3, 5)
# print(result)  # Output will be 8


square=lambda x: x * x
print(square(4))  # Output will be 16
# This lambda function takes one argument x and returns its square.